# Server only. Never use VITE_ prefixed variables for credentials.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-astra
PORT=3001
HOST=127.0.0.1
# Required before binding a public interface. A local, single-user access gate, not team auth.
LOOPHOLE_ACCESS_TOKEN=
